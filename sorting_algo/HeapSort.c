#include <stdio.h>
#include <stdlib.h>

int main()
{
    int arr[6] = {5, 9, 8, 7, 1, 3};
    int n = sizeof(arr)/sizeof(int);
    left = arr[0];
    right = arr[n];
    pivot = arr[0];
    for (int i = 0; i<n; i++)
    {
        if (pivot < right)
        {
            
        }
    }
}